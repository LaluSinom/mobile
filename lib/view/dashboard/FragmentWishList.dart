import 'package:flutter/material.dart';

class FragmentWishlist extends StatelessWidget {
  const FragmentWishlist({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Wishlist'),
      ),
    );
  }
}
